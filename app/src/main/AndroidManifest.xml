<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".memoDB.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._0401ChkMyPlan"
        tools:targetApi="31">
        <activity
            android:name=".PopupActivity"
            android:theme="@style/Theme.AppCompat.Dialog"/>
        <activity
            android:name=".LocationSetActivity"
            android:exported="false" />
        <activity
            android:name=".MemoDetailActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".activity_memo_detail" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".ActivityDetailSet"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".activity_detail_set" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".CheckMyPlanSplash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>